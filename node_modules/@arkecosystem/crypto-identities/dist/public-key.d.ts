import { MultiSignatureAsset } from "./contracts";
export declare class PublicKey {
    static fromPassphrase(passphrase: string): string;
    static fromWIF(wif: string, options: {
        wif: number;
    }): string;
    static fromMultiSignatureAsset(asset: MultiSignatureAsset): string;
    static verify(publicKey: string): boolean;
}
