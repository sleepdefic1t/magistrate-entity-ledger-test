import { KeyPair } from "./contracts";
export declare class WIF {
    static fromPassphrase(passphrase: string, options: {
        wif: number;
    }): string;
    static fromKeys(keys: KeyPair, options: {
        wif: number;
    }): string;
}
