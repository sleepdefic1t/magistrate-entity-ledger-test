/// <reference types="node" />
import { KeyPair } from "./contracts";
export declare class Keys {
    static fromPassphrase(passphrase: string, compressed?: boolean): KeyPair;
    static fromPrivateKey(privateKey: Buffer | string, compressed?: boolean): KeyPair;
    static fromWIF(wif: string, options: {
        wif: number;
    }): KeyPair;
}
