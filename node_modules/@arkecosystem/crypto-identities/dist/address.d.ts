/// <reference types="node" />
import { MultiSignatureAsset, KeyPair } from "./contracts";
export declare class Address {
    static fromPassphrase(passphrase: string, options: {
        pubKeyHash: number;
    }): string;
    static fromPublicKey(publicKey: string, options: {
        pubKeyHash: number;
    }): string;
    static fromWIF(wif: string, options: {
        pubKeyHash: number;
        wif: number;
    }): string;
    static fromMultiSignatureAsset(asset: MultiSignatureAsset, options: {
        pubKeyHash: number;
    }): string;
    static fromPrivateKey(privateKey: KeyPair, options: {
        pubKeyHash: number;
    }): string;
    static fromBuffer(buffer: Buffer): string;
    static toBuffer(address: string, options: {
        pubKeyHash: number;
    }): {
        addressBuffer: Buffer;
        addressError?: string;
    };
    static validate(address: string, options: {
        pubKeyHash: number;
    }): boolean;
}
