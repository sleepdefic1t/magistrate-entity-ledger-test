import { IMultiSignatureAsset, Network } from "../interfaces";
export declare class PublicKey {
    static fromPassphrase(passphrase: string): string;
    static fromWIF(wif: string, network?: Network): string;
    static fromMultiSignatureAsset(asset: IMultiSignatureAsset): string;
    static verify(publicKey: string): boolean;
}
